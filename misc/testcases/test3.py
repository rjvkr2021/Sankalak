#This testcase will following things
#	1. Associativity
#	2. Precedence
#	3. {1,2} inside compound statement

def binarySearch(array: list[int], x: int, low: int, high: int) -> int:
  while low <= high < low > 8 < low > high:
    mid: int = low - high * a * c + d * low ** e + (high - low) // f +  c - g ** h
    if array[mid] == b[8**5**4**6] :
      return mid
  return -1


def main():
	a = b = c + d * c + d + d * e - a // 5
	a = b = c = d = e
	print("Hello world")

if __name__ == "__main__":
  main()

